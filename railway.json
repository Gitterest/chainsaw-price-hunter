# Update railway.json to use the correct Dockerfiles
updated_railway_content = """
{
	"services": [
    {
      "name": "frontend",
      "root": "frontend",
      "dockerfile": "Dockerfile.frontend",
      "env": ".env"
    },
    {
      "name": "backend",
      "root": "backend",
      "dockerfile": "Dockerfile.backend",
      "env": ".env"
    }
  ]
}
"""

# Write the updated files
with open(frontend_dockerfile_path, 'w', encoding='utf-8') as file:
    file.write(frontend_dockerfile_content)

with open(backend_dockerfile_path, 'w', encoding='utf-8') as file:
    file.write(backend_dockerfile_content)

with open(railway_path, 'w', encoding='utf-8') as file:
    file.write(updated_railway_content)

"Updated Dockerfiles and railway.json successfully."