{
  "services": [
    {
      "name": "frontend",
      "rootDirectory": "frontend",
      "buildCommand": "npm install && npm run build",
      "startCommand": "npx serve@latest out -p 3000"
    },
    {
      "name": "backend",
      "rootDirectory": "backend",
      "buildCommand": "npm install",
      "startCommand": "node server.js"
    }
  ]
}
